pipeline {
agent any

```
stages {
    stage('Checkout') {
        steps {
            echo 'Code checked out successfully'
        }
    }

    stage('Run Tests') {
        steps {
            sh 'python --version'
        }
    }
}
```

}